# Android build output
src/.gradle/
src/app/build/
src/build/

# Local config (machine-specific)
src/local.properties

# IDE
src/.idea/
*.iml

# Keys / signing
*.jks
*.keystore
# ...but the capy release keystore is committed intentionally so any machine can
# build a permanent (non-expiring) signed release. Keep it private to this repo.
!src/app/capy-release.jks

# OS
.DS_Store
